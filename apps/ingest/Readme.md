# @otm/ingest (Fluid)

- Runtime: Vercel Fluid (Node.js 22)
- Local: `vercel dev --listen 4000` (expect 404 until routes land in S5)
- Routes live under `api/*` (TypeScript supported by `vercel.json`)
