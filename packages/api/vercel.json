{
  "version": 2,
  "builds": [
    {
      "src": "dist/index.js",
      "use": "@vercel/node",
      "config": { "includeFiles": ["../../packages/**"] }
    }
  ],
  "routes": [{ "src": "/api/trpc(?:/.*)?", "dest": "dist/index.js" }],
  "env": {
    "NODE_ENV": "production"
  }
}
