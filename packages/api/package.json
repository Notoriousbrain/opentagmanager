{
  "name": "@otm/api",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "clean": "rm -rf dist",
    "build": "bun build ./src/index.ts ./src/health.ts --outdir ./dist --target=node --format=esm"
  },
  "dependencies": {
    "@trpc/server": "catalog:",
    "nanoid": "^5.1.6",
    "superjson": "catalog:",
    "zod": "^catalog:"
  }
}
