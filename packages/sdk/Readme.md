# @otm/sdk

Universal JavaScript client SDK for OSSTag.

This package provides:
- `createClient()` for browser & Node usage
- canonical JSON serializer
- HMAC signing helpers
- batching utilities for transports
- framework integrations (React, Next, etc.)

> Full documentation coming soon.
