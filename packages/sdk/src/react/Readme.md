# @otm/sdk/react

React integration for the OSSTag client SDK.

Features:
- `OTMProvider` for automatic client setup
- `useOTM()` hook to access the client
- Framework-aware batching and lifecycle helpers

> Full documentation coming soon.
